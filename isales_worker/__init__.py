"""isales-worker: post-call async post-processor.

Spec: architecture § isales-worker; retry-followup § scheduler 调度数据流
      (worker write-back); webhook-callback (full); goal-achievement
      (worker doesn't re-judge); transcript; service-communication.
"""

__version__ = "0.1.0"
